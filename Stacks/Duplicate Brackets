bool findDuplicateparenthesis(string str)
{
    
    stack<char> Stack;
    for (char ch : str)
    {
        if (ch == ')')
        {
            if(ch=='('){return true;}
            else
            { while(ch!='(')
            { st.pop(); }
        }
        st.pop();
        else
            Stack.push(ch);
    }
 
    // No duplicates found
    return false;
}
